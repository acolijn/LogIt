# Minimal requirements for building documentation on ReadTheDocs
# Only mock imports are needed - we don't actually run the code

# Sphinx and theme
sphinx>=7.0.0
sphinx-rtd-theme>=2.0.0

# Mock these packages - Sphinx autodoc needs to import them but won't run them
Flask>=3.0.0
Flask-Login>=0.6.0
Flask-PyMongo>=3.0.0
Flask-WTF>=1.2.0
pymongo>=4.0.0
Werkzeug>=3.0.0
WTForms>=3.0.0
Authlib>=1.3.0
pytz>=2023.0
python-dateutil>=2.8.0
plotly>=5.0.0

